# Docker Desktop


```javascript
// first time we have to install in mac 
- Install via Homebrew:

brew install --cask docker
open /Applications/Docker.app

// Then open the Docker app, allow permissions, wait until the Docker icon shows "running".


// Verify:
docker --version
docker compose version
docker run --rm hello-world

after creating the docker-compose.yml file we have to run this command 
`docker compose up` //our container is running now 
```